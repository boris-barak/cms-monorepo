import { Divider, Paragraph, Section } from 'cms-common/types/page';

export const initialSection: Section = {
    hash: '',
    header: '',
    items: [],
};

export const initialParagraph: Paragraph = {
    type: 'paragraph',
    content: '',
};

export const initialDivider: Divider = {
    type: 'divider',
};
